# Apex Bike Release Assets

Portal assets for the first EvenHub submission.

- `apexbike-icon.png`: square monochrome/greyscale app icon.
- `apexbike-background.png`: monochrome/greyscale listing/background image.
- `apexbike-glasses-home-screenshot.png`: simulator screenshot from the built-in glasses capture API.

The current EvenHub CLI packs only `app.json` and the built `dist` folder into the `.ehpk`; these images are kept next to the package for manual portal upload.
